#!/usr/bin/env murex

function demo {
  value = %{
    name: %("murex")
    tags: %["a" "b" %{"nested": true}]
  }

  %[1..3] -> foreach i {
    out "idx=$i"
  }

  %[Monday..Sunday] -> [{ $.v =~ "^S" && out "weekend: $.v" }]

  out ${value}
  out @{match "x"}
  out <stdin> -> match "foo"
}
